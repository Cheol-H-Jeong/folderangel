__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/
build/
dist/
.venv/
venv/
env/
.coverage
.coverage.*
.pytest_cache/
.mypy_cache/
.ruff_cache/
.idea/
.vscode/
*.spec
!scripts/folder1004.spec
*.db
.DS_Store
Thumbs.db
Folder1004_Report_*.md
Folder*_Report_*.md

# --- Local user data: NEVER commit ---
# Per-run logs (the app writes these under ~/.folder1004/logs/, but
# defensively also block any *.log anywhere in the tree in case a user
# copies one in for debugging).
*.log
logs/
.folder1004/
.folder*/
folder1004_logs/
folder*_logs/

# Saved settings / API keys / endpoints — both the app's canonical
# locations and any common stray names.
config.json
.env
.env.*
*.env
.envrc
secrets.*
api_key*
*.api_key
*.token
keyring.*

# SQLite indexes (already covered by *.db, kept for clarity)
index.db*
